# jarvis-core 의존성
# 아직 미구현 모듈의 의존성은 해당 STEP에서 추가

# STEP 7 — 음성 입출력
faster-whisper>=1.0.0
edge-tts>=6.1.0
sounddevice>=0.4.6
numpy>=1.24.0
pygame>=2.5.0           # tts 합성 결과(mp3) 재생
torch>=2.0.0            # silero-vad 추론용
silero-vad>=5.1

# STEP 8 — 핫워드
openwakeword>=0.6.0

# STEP 10 — UI 서버
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
psutil>=5.9.0

# STEP 15+ — 스킬 확장
pycaw>=20240210         # skill_volume (Windows 전용)
comtypes>=1.4.16         # skill_volume (pycaw 의존). 1.4.6은 Python 3.13에서 NameError로 깨짐 (우선순위 2 검증 중 발견)
pygetwindow>=0.0.9       # skill_window (Windows 전용)
pyperclip>=1.8.2         # skill_clipboard

# 유틸리티
pyyaml>=6.0

# Claude Code CLI 엔진 (core/engines/claude_cli_engine.py)
# claude CLI는 npm으로 별도 설치: npm install -g @anthropic-ai/claude-code
python-dotenv>=1.0.0

# 웹 검색 (core/search_engine.py, skills/skill_web_search.py)
ddgs>=9.0.0              # duckduckgo_search의 후속 패키지 (구버전은 deprecated)
requests>=2.31.0         # Brave Search 폴백용

# 신규 스킬 확장 (JARVIS_SKILLS_ARCHITECTURE.md 묶음 B)
pyjokes>=0.6.0           # skill_joke
qrcode[pil]>=7.4         # skill_qr

# 신규 스킬 확장 (묶음 E)
yt-dlp>=2024.1           # skill_youtube (pytube는 deprecated)

# 신규 스킬 확장 (묶음 F)
opencv-python>=4.9       # skill_camera (IP 카메라 스트림 캡처)

# 신규 스킬 확장 (묶음 G)
pypdf>=4.0               # skill_pdf_reader (PyPDF2는 deprecated)
pywhatkit>=5.4           # skill_whatsapp (GUI 자동화 의존성 동반)

# 달력 스킬 — 한국 공휴일·대체공휴일·음력 연휴 자동 계산
holidays>=0.45           # skill_calendar (설날/추석 음력 포함, 대체공휴일 지원)

# Llama 에이전트 도구 (skills/agent_tools/ — Stage 1)
beautifulsoup4>=4.12     # agent_tools/browser_tool (HTML 파싱)
openpyxl>=3.1            # agent_tools/file_tool (xlsx 저장)

# 화면 제어 하이브리드 에이전트 (core/hybrid_screen.py + skills/skill_screen_agent.py)
# 전략 A: UIA 트리(정밀 좌표) + Claude Vision(맥락 이해) 동시 실행
pyautogui>=0.9.54        # 스크린샷 캡처 + 마우스·키보드 제어
Pillow>=10.0             # SoM 번호 오버레이 이미지 생성
uiautomation>=2.0.18     # Windows UI Automation API — 요소 트리·좌표·상태 접근

# 웹 수집 에이전트 (core/web_collector.py + skills/skill_web_collector.py)
# 전략: Playwright DOM 스냅샷(텍스트) + Claude 판단 — 스크린샷/Vision 없이 관찰-판단-실행
playwright>=1.42        # 실제 브라우저(Chromium) 제어 — 최초 1회 `playwright install chromium` 별도 필요
