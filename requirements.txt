# jarvis-core 의존성
# 아직 미구현 모듈의 의존성은 해당 STEP에서 추가

# STEP 7 — 음성 입출력
faster-whisper>=1.0.0
edge-tts>=6.1.0
sounddevice>=0.4.6
numpy>=1.24.0
pygame>=2.5.0           # tts 합성 결과(mp3) 재생
torch>=2.0.0            # silero-vad 추론용
silero-vad>=5.1

# STEP 8 — 핫워드
openwakeword>=0.6.0

# STEP 10 — UI 서버
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
psutil>=5.9.0

# STEP 15+ — 스킬 확장
pycaw>=20240210         # skill_volume (Windows 전용)
comtypes>=1.2.0          # skill_volume (pycaw 의존)
pygetwindow>=0.0.9       # skill_window (Windows 전용)
pyperclip>=1.8.2         # skill_clipboard

# 유틸리티
pyyaml>=6.0
